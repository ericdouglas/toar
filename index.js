'use strict';

module.exports = toar;

function toar( input, splitter ) {
  var array = [];
  var key;
  var type = typeof input;
 
  // Verify if is an array
  // Verify if is an object but is falsy (null or undefined)
  // Dynamic dispatch

}
